version: '3'

services:
  php:
    build: .
    ports:
      - "8000:80"
    volumes:
      - .:/var/www/html

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    ports:
      - "8001:80"
    environment:
      - PMA_HOST=47.106.115.51
      - PMA_USER=haitun
      - PMA_PASSWORD=cFzMv0Ha^&EIz&g6Va
      - PMA_PORTS=3307
